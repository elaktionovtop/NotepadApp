<Window x:Class="NotepadApp.Views.Dialogs.InputLineDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding TitleText}" 
        Height="180" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <StackPanel Margin="10">
        <TextBlock Text="{Binding Message}" Margin="0,0,0,5"/>
        <TextBox x:Name="inputBox"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,5"/>

        <TextBlock Text="{Binding ErrorText}"
                   Foreground="Red" Margin="0,0,0,5"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="70" Margin="0,0,5,0"
                    IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="70" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
